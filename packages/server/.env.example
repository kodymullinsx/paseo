# OpenAI API Key (for GPT-4, Whisper STT, and TTS)
OPENAI_API_KEY=

# DeepInfra API key (optional, enables first-class DeepInfra provider)
DEEPINFRA_API_KEY=
# Optional overrides for DeepInfra API behavior
# Default base URL for the first-class provider is https://api.deepinfra.com/v1/openai
# DEEPINFRA_API_BASE_URL=https://api.deepinfra.com/v1/openai
# DEEPINFRA_MODELS_URL=https://api.deepinfra.com/v1/openai/models
# DEEPINFRA_DEFAULT_MODEL=meta-llama/Meta-Llama-3.1-8B-Instruct

# TTS Configuration (optional - defaults shown)
TTS_VOICE=alloy
# Available voices: alloy, echo, fable, onyx, nova, shimmer
TTS_MODEL=tts-1
# Available models: tts-1 (faster), tts-1-hd (higher quality)

# Server Configuration
# Location for runtime state (agents.json, etc.). Defaults to ~/.paseo
PASEO_HOME=~/.paseo

# Listen address. Defaults to 127.0.0.1:6767
# Can be host:port, /path/to/socket, or unix:///path/to/socket
PASEO_LISTEN=127.0.0.1:6767

# Debug recordings (dictation + STT input + TTS output)
# When enabled, recordings are saved under `${cwd}/.debug/recordings/`
PASEO_DICTATION_DEBUG=1

# Agent provider policy (optional)
# Default provider policy is Nanoclaw-only.
# Override with comma-separated provider IDs:
# - PASEO_ENABLED_AGENT_PROVIDERS=all
# - PASEO_ENABLED_AGENT_PROVIDERS=nanoclaw,deepinfra
# - PASEO_ENABLED_AGENT_PROVIDERS=none
# Valid IDs: claude, deepinfra, codex, opencode, nanoclaw
# PASEO_ENABLED_AGENT_PROVIDERS=nanoclaw

# Mem0 long-term memory (optional)
# Requires `mem0ai` + `@qdrant/js-client-rest` installed in this workspace.
# Set PASEO_MEM0_ENABLED=0 to disable even if credentials are set.
# PASEO_MEM0_ENABLED=1
# PASEO_MEM0_QDRANT_URL=https://<your-cluster>.cloud.qdrant.io
# PASEO_MEM0_QDRANT_API_KEY=<qdrant-api-key>
# PASEO_MEM0_COLLECTION_NAME=openclaw-memory
# PASEO_MEM0_OLLAMA_BASE_URL=http://127.0.0.1:11434
# PASEO_MEM0_EMBEDDING_MODEL=qwen3-embedding:8b-fp16
# PASEO_MEM0_DEDUP_MODEL=gpt-oss:20b
# PASEO_MEM0_USER_ID=user
# Default is shared user ID across channels for unified memory continuity.
# Set PASEO_MEM0_SCOPE_BY_CWD=1 only if you want per-workspace isolation.
# PASEO_MEM0_SCOPE_BY_CWD=0
